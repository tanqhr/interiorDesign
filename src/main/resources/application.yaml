spring:
  datasource:
    url: jdbc:mysql://localhost:3306/interior_design
    username: root
    password: 12345

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  messages:
    basename: i18n/messages
    encoding: UTF-8

app:
    upload-dir: uploads