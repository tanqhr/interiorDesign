spring:
  datasource:
    url: jdbc:mysql://localhost:3306/interior_design
    username: root
    password: 12345

  mail:
      host: smtp.abv.bg
      port: 465
      username: mail_000@abv.bg
      password: Mail_000@012

      properties:
        mail:
          smtp:
            auth: true
            ssl:
              enable: true

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  messages:
    basename: i18n/messages
    encoding: UTF-8

app:
    upload-dir: uploads